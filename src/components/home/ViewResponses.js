import React from 'react';

const ViewResponses = () => {
    // Component logic for viewing responses

    return (
        <div>
            <h2>View Responses</h2>
            {/* Display responses for queries posted by the customer */}
        </div>
    );
};

export default ViewResponses;
